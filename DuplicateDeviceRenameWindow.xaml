<Window x:Class="DanteConfigEditor.DuplicateDeviceRenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Doublons de machines"
        Width="720"
        Height="460"
        MinWidth="620"
        MinHeight="360"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="IntroTextBlock"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Label x:Name="SuffixLabel" Content="Suffixe du renommage automatique" VerticalAlignment="Center" />
            <TextBox x:Name="SuffixTextBox" Width="220" Margin="10,0,0,0" Text="Import" TextChanged="SuffixTextBox_TextChanged" />
        </StackPanel>

        <DataGrid x:Name="RenameGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  ItemsSource="{Binding RenameItems}">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="OriginalNameColumn"
                                    Binding="{Binding OriginalName}"
                                    IsReadOnly="True"
                                    Width="*" />
                <DataGridTextColumn x:Name="NewNameColumn"
                                    Binding="{Binding NewName, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <WrapPanel Grid.Row="3"
                   HorizontalAlignment="Right"
                   Margin="0,14,0,0">
            <Button x:Name="UniqueOnlyButton"
                    MinWidth="150"
                    Margin="0,0,8,0"
                    Click="UniqueOnlyButton_Click" />
            <Button x:Name="AutoRenameButton"
                    MinWidth="150"
                    Margin="0,0,8,0"
                    Click="AutoRenameButton_Click" />
            <Button x:Name="ManualRenameButton"
                    MinWidth="160"
                    Margin="0,0,8,0"
                    Click="ManualRenameButton_Click" />
            <Button x:Name="CancelImportButton"
                    MinWidth="90"
                    Click="CancelImportButton_Click" />
        </WrapPanel>
    </Grid>
</Window>
